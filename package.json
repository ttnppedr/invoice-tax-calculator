{
  "name": "invoice-tax-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=invoice-ii-wa"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "wrangler": "^4.128.0"
  }
}
